import { StudyTile } from "./study-tile.jsx";

export default {
  title: "Homepage/StudyTile",
  component: StudyTile,
};

const Template = (args) => <StudyTile {...args} />;

export const Default = Template.bind({});