import { PartnerSection } from "./partner-section.jsx";

export default {
  title: "Homepage/PartnerSection",
  component: PartnerSection,
};

const Template = (args) => <PartnerSection {...args} />;

export const Default = Template.bind({});