import { Swipers } from "./swiper.jsx";

export default {
  title: "Homepage/Swiper",
  component: Swipers,
};

const Template = (args) => <Swipers {...args} />;

export const Default = Template.bind({});