import { Studies } from "./studies.jsx";

export default {
  title: "Homepage/Studies",
  component: Studies,
};

const Template = (args) => <Studies {...args} />;

export const Default = Template.bind({});